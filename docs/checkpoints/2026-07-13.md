# 2026-07-13 — Codex対応

## 目的

Claude Code専用だったcadenceを、兄弟リポ`claude-rules` / `quorum`と同じ配布契約でCodexからも実行可能にする。

## 実装

1. `AGENTS.md` / `REQUIREMENTS.md` / `NOTES.md`を追加し、両ホスト要件・Git契約・配置先移行の未決事項を4軸へ集約。
2. `skills/codex-cadence/SKILL.md`と`agents/openai.yaml`を追加。Codexの直接サブエージェント、`apply_patch`、web、shell、MCPへフローの能力ラベルを写像し、`$quorum`連携・有人承認・収束規約を明記。
3. `install.sh`を両ホスト対応にし、共有flows/references/hookへCodex固有SKILL/UIメタデータを重ねて`$CODEX_HOME/skills/cadence`を生成。`rules/codex-cadence-triage.md`をグローバル`AGENTS.md`へ冪等注入。
4. PJローカル資産の解決順を`.agents/cadence`→`.claude/cadence`→同梱へ変更。validatorも名前単位の優先解決と両ホストpersona fallbackへ対応。
5. readonly hookをCodexの`apply_patch`入力へ対応。patch headerから全対象を抽出し、`.cadence/`だけなら許可、混在・path traversal・解析不能はセンチネル存在中fail closed。Codex hookのshell遮断限界も文書化。
6. `optimize-context`とflow-authoringをCLAUDE/AGENTS両ホスト表記へ一般化。README/HANDOFF/fixturesも両ホスト手順へ更新。
7. 初回forward-testで領域分割をまたぐDB alert欠落を見逃したため、runbook↔paging-alertの双方向対応表をplan/audit/superviseとSRE personaへ追加。横断要件は対応表で固定する判断を`NOTES.md`へ記録。

## 検証

- `python3 scripts/validate-flows.py`: 同梱5フローすべてOK。
- `bash tests/run_tests.sh`: 15件OK。対象は両ホストinstallの冪等性・既存指示保持、`.agents`優先/`.claude`互換、Claude/Codex hook、path traversal、mixed/move patch、横断フロー契約。
- Codex skill validator: source stagingと実インストール先の両方でOK。
- 一時`CLAUDE_CONFIG_DIR` / `CODEX_HOME`へ2回installし、skill・UI metadata・共有資産・symlink・markerが一致することを確認。
- 実環境へ`./install.sh`を適用し、`~/.codex/skills/cadence`とグローバルCADENCE接続を確認。管理対象トリアージ文を意味保持で圧縮し、グローバルCLAUDE.mdを14KB以内（14174 bytes）へ戻した。
- fresh agentによる合成フィクスチャ実走：`START→plan→audit→supervise→COMPLETE`、全13資料、17 findings、supervise 1/3、外部MCP/network未使用、非`.cadence` SHA-256完全一致、readonlyセンチネルcleanupを確認。
- 初回forward-testの採点は既知14項目中13項目を検出。DB runbookのalert trigger欠落とalertのrunbook参照確認が横断ギャップだったため上記対応表を追加。改善後の再実走はモデル容量エラーで完走できず、一時成果物は削除済み。契約自体は決定論テストで固定した。

## 残り

- モデル容量回復後、改善後のfresh forward-testを再実走する。
- 実infra MCP、review/ABORT、gates、quorum外注は従来どおり未検証。
